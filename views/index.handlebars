
         <header class="default-menu">
            <main>
                <div class="header-1">

            
                <div class="start">
                <img src="./images/start.png" width="40" height="36"/>
                </div>
                     <div class="nome-professor">
                        <img src="./images/nomeSite.png" width="125" height="28"/>    
                     </div>
                </div>
            </main>
        </header> 
        <main class="col urls">
            <div class="header-2">
                <div class="menu"> 
                    <ul>
                        <li>
                            <a href="/">Pagina Inicial</a>
                        </li>
                        {{#if user}}
                        <li>
                            <a href="redacoesCorrigidas">Minhas Redações</a>
                        </li>
                         <li>
                            <a href="redacaoSend">Enviar Redação</a>
                        </li>
                        <li>
                            <a href="crudAluno">Conta</a>
                        </li>                                                
                        <li>
                            <a href="/logout">Sair</a>
                        </li>
                        {{else}}
                        <li>
                            <a href="areaAluno">Login</a>
                        </li>
                        <li>
                            <a href="registro">Registrar-se</a>
                        </li>
                        {{/if}}
                    </ul>
                </div>
            </div>
        </main>
        <div class="col-100"style="height: 1000px;">
            <div class="redacoes">
                <br>
                    <h2>Redações</h2>
                    <div class="coluna-1">
                        {{#each redacoes}}
                        <input type="hidden" name="id" id="{{_id}}">
                        <div class="card">
                        <div class="card-body1">
                            <p class="card-text">{{texto}}</p>
                           <a href="/redacao/{{_id}}"><button class="btn btn-primary">Leia mais...</button></a><br>
                        </div>
                        </div>
                        {{else}}
                        <p>Nenhuma redacao registrada!</p>
                        {{/each}}              
                    </div>
            </div>
        </div>  